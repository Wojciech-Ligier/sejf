{
  "name": "sejf",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "if [ -z \"$CLOUDFLARE_API_TOKEN\" ]; then echo 'Skipping Cloudflare deploy: CLOUDFLARE_API_TOKEN not set' && exit 0; else wrangler pages deploy dist; fi",
    "lint": "eslint . --max-warnings=0",
    "format": "prettier -w ."
  },
  "devDependencies": {
    "typescript-eslint": "^7.0.0",
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "wrangler": "^3.27.0"
  }
}
